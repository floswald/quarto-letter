$if(closing)$
\closing{$closing$,\\[1cm]
$if(signature)$
\includegraphics[height=1.5cm]{$signature$}
\vspace{0.5cm}
Florian Oswald
$endif$
}
$endif$
$if(encl)$
\encl{$for(encl)$$encl$$sep$\\$endfor$}
$endif$
$if(cc)$
\cc{$for(cc)$$cc$$sep$\\$endfor$}
$endif$
$if(ps)$
\ps{$ps$}
$endif$
\end{letter}
